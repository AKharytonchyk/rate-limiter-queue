{
  "name": "rate-limiter-queue",
  "version": "1.0.3",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc"
  },
  "author": {
    "name": "Artsiom Kharytonchyk",
    "url": "https://github.com/AKharytonchyk/"
  },
  "description": "A TypeScript-based rate limiter queue for managing API requests. This queue allows you to set a maximum number of requests per minute and enqueues additional requests, processing them as allowed by the rate limit.",
  "keywords": ["rate-limiter", "queue", "API", "rate-limiting", "TypeScript"],
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
